{{ 'button-style.css' | asset_url | stylesheet_tag }}
<link href="{{ 'custom-effect.css' | asset_url }}" rel="stylesheet" media="print" onload="this.media='all'">
{%- liquid
  assign section_blocks = section.blocks
  assign se_stts = section.settings
  assign ccount = cart.item_count
  assign ck_lumise = 'ck_lumise'
  assign cart_url = routes.cart_url
  assign cart_change_url = routes.cart_change_url
  assign min_qty = settings.min_qty | plus: 0
  assign compare_tt_price = 0
  assign shipping_amount = settings.free_ship_pr.price | default: -1
  assign total_price = cart.total_price

  assign se_style = se_stts.style
  assign se_pos = se_stts.pos

  assign gift_pr = all_products[settings.gift_wrap_pr]
  assign gift_pr_id = gift_pr.id
  assign edit_item = settings.edit_item
-%}

<link rel="stylesheet" href="{{ 'drawer.min.css' | asset_url }}" media="all">
<link rel="stylesheet" href="{{ 'mini-cart.css' | asset_url }}" media="all">
<svg class="t4s-d-none">
  <symbol id="icon-cart-remove" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path><line x1="10" y1="11" x2="10" y2="17"></line><line x1="14" y1="11" x2="14" y2="17"></line>
  </symbol>
  <symbol id="icon-cart-edit" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
    <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
  </symbol>
  <symbol id="icon-cart-tag" viewBox="0 0 448 512">
    <path d="M48 32H197.5C214.5 32 230.7 38.74 242.7 50.75L418.7 226.7C443.7 251.7 443.7 292.3 418.7 317.3L285.3 450.7C260.3 475.7 219.7 475.7 194.7 450.7L18.75 274.7C6.743 262.7 0 246.5 0 229.5V80C0 53.49 21.49 32 48 32L48 32zM112 176C129.7 176 144 161.7 144 144C144 126.3 129.7 112 112 112C94.33 112 80 126.3 80 144C80 161.7 94.33 176 112 176z"/>
  </symbol>
  <symbol id="icon-cart-spinner" viewBox="0 0 66 66">
    <circle class="t4s-path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
  </symbol>
  <symbol id="icon-cart-check" viewBox="0 0 448 512">
    <path d="M443.3 100.7C449.6 106.9 449.6 117.1 443.3 123.3L171.3 395.3C165.1 401.6 154.9 401.6 148.7 395.3L4.686 251.3C-1.562 245.1-1.562 234.9 4.686 228.7C10.93 222.4 21.06 222.4 27.31 228.7L160 361.4L420.7 100.7C426.9 94.44 437.1 94.44 443.3 100.7H443.3z"/>
  </symbol>
  <symbol id="icon-cart-selected" viewBox="0 0 24 24">
  <path d="M9 20l-7-7 3-3 4 4L19 4l3 3z"></path>
  </symbol>
</svg>

<div
  data-cart-wrapper
  id="t4s-mini_cart"
  class="t4s-drawer t4s-drawer__right t4s-dn"
  aria-hidden="true"
>
  <div class="t4s-drawer__header">
    <span class="--header-spacer"></span>
    <p class="--cart-title">{{ 'cart.mini_cart.title' | t }}</p>
    <button class="t4s-drawer__close" data-drawer-close aria-label="{{'cart.mini_cart.close_cart' | t}}">
      {%- comment -%} PC 保持母主题原有的圆角方框图标 {%- endcomment -%}
      <svg class="--close-pc" width="100%" height="100%" viewBox="0 0 65 66" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M52.8 65.5H13.2C6.70002 65.5 1.40002 60.2 1.40002 53.7V13.2C1.40002 6.69999 6.70002 1.39999 13.2 1.39999H52.7C59.2 1.39999 64.5 6.69999 64.5 13.2V53.7C64.5 60.3 59.3 65.5 52.8 65.5Z" fill="#1E1E1E" stroke="#666666" stroke-width="0.9603" stroke-miterlimit="10"></path>
        <path d="M21.7001 22L44.3001 45" stroke="#828282" stroke-width="3.8412" stroke-miterlimit="10"></path>
        <path d="M44.3001 22L21.7001 45" stroke="#828282" stroke-width="3.8412" stroke-miterlimit="10"></path>
      </svg>
      {%- comment -%} 移动端按设计稿用细线 X {%- endcomment -%}
      <svg class="--close-mb" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M5 5L19 19"></path>
        <path d="M19 5L5 19"></path>
      </svg>
    </button>
  </div>
  {%- if se_stts.enable_calc_ship -%}
    {%- assign shipping_money = shipping_amount | money -%}
    <link rel="stylesheet" href="{{ 'shipping_bar.css' | asset_url }}" media="all">
    <div data-cart-calc-shipping class="t4s-cart__threshold">
      {%- if total_price == 0 -%}
        {%- capture style_w %} style="width:0"{% endcapture -%}
        <div data-cart-ship-text class="t4s-cart__thres1">
          {{ 'cart.shipping_threshold.text_1_html' | t: money: shipping_money }}
        </div>
      {%- elsif shipping_amount > total_price -%}
        {%- assign space_money = shipping_amount | minus: total_price | money -%}
        <div data-cart-ship-text class="t4s-cart__thres2">
          {{ 'cart.shipping_threshold.text_2_html' | t: money: space_money }}
        </div>
        {%- capture style_w %} style="width: calc(100% - {{shipping_amount | minus: total_price | times: 100.0 | divided_by: shipping_amount}}% )"{% endcapture -%}
      {%- else -%}
        <div data-cart-ship-text data-cart-ship-done class="t4s-cart__thres3">
          {{
            'cart.shipping_threshold.text_3'
            | t
            | replace: '[', '<span class="t4s-cr is--congratulations">'
            | replace: ']', '</span>'
          }}
        </div>
        {%- capture style_w %} style="width:100%"{% endcapture -%}
      {%- endif -%}
      {%- if settings.enable_shipbar %}
        <div class="t4s-cart-thes__bar bgt4_svg{{settings.des_shipbar}} t4s-pr">
          <span data-cart-ship-bar class="t4s-pr t4s-d-block t4s-h-100" {{ style_w }}>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 640 512">
              <path class="fa-primary" d="M64 48C64 21.49 85.49 0 112 0H368C394.5 0 416 21.49 416 48V96H466.7C483.7 96 499.1 102.7 512 114.7L589.3 192C601.3 204 608 220.3 608 237.3V352C625.7 352 640 366.3 640 384C640 401.7 625.7 416 608 416H574.9C567.1 361.7 520.4 320 464 320C407.6 320 360.9 361.7 353.1 416H286.9C279.1 361.7 232.4 320 176 320C127.9 320 86.84 350.4 70.99 392.1C66.56 385.7 64 377.1 64 368V256H208C216.8 256 224 248.8 224 240C224 231.2 216.8 224 208 224H64V192H240C248.8 192 256 184.8 256 176C256 167.2 248.8 160 240 160H64V128H272C280.8 128 288 120.8 288 112C288 103.2 280.8 96 272 96H64L64 48zM544 256V237.3L466.7 160H416V256H544z"/><path class="t4s-ship-secondary" d="M272 128H16C7.164 128 0 120.8 0 112C0 103.2 7.164 96 16 96H272C280.8 96 288 103.2 288 112C288 120.8 280.8 128 272 128zM240 160C248.8 160 256 167.2 256 176C256 184.8 248.8 192 240 192H48C39.16 192 32 184.8 32 176C32 167.2 39.16 160 48 160H240zM208 224C216.8 224 224 231.2 224 240C224 248.8 216.8 256 208 256H16C7.164 256 0 248.8 0 240C0 231.2 7.164 224 16 224H208zM256 432C256 476.2 220.2 512 176 512C131.8 512 96 476.2 96 432C96 387.8 131.8 352 176 352C220.2 352 256 387.8 256 432zM544 432C544 476.2 508.2 512 464 512C419.8 512 384 476.2 384 432C384 387.8 419.8 352 464 352C508.2 352 544 387.8 544 432z"/>
            </svg>
          </span>
        </div>
      {% endif -%}
    </div>
  {%- endif -%}
  <form action="{{ cart_url }}" method="post" novalidate class="t4s-drawer__wrap">
    <input type="hidden" data-cart-attr-rm name="attributes[collection_items_per_row]" value="">
    <div class="t4s-drawer__main">
      <div data-t4s-scroll-me class="t4s-drawer__scroll t4s-current-scrollbar hide-scrollbar">
        <div class="t4s-cookie-message t4s-dn">{{ 'cart.general.cookies_required' | t }}</div>
        <div data-cart-items class="t4s-mini_cart__items t4s_ratioadapt t4s-product">
          {%- if ccount > 0 -%}
            {%- for item in cart.items -%}
              {%- render 'cart-item',
                item: item,
                size: forloop.length,
                index: forloop.index,
                gift_pr_id: gift_pr_id,
                min_qty: min_qty,
                compare_tt_price: compare_tt_price,
                cart_change_url: cart_change_url,
                edit_item: edit_item
              -%}
            {%- endfor -%}
            <div data-personalization-id="00007" class=" apz-widget-root-container"></div>
          {%- else -%}
            <style>
              #t4s-mini_cart .t4s-drawer__bottom {
                opacity: 0;
                -webkit-transform: translateY(45px);
                transform: translateY(45px);
                -webkit-transition: opacity 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94),
                  -webkit-transform 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
                transition: opacity 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94),
                  transform 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94),
                  -webkit-transform 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
              }
            </style>
            <div class="t4s-mini_cart__emty">
              <p class="--empty-text">{{ 'cart.mini_cart.empty' | t }}</p>
              {%- assign btn_blocks = section_blocks | where: 'type', 'btn' -%}
              {%- if btn_blocks.size > 0 -%}
                {%- for block in btn_blocks -%}
                  {%- assign bk_stts = block.settings -%}
                  {%- assign button_style = bk_stts.button_style -%}
                  {%- if bk_stts.title != blank -%}
                    <p class="t4s-return-to-shop">
                      <a
                        data-loading-bar
                        class="--empty-btn"
                        href="{{'/collections/electric-bike'}}"
                      >
                        {{- bk_stts.title }}
                        {%- if bk_stts.btn_icon -%}
                          <svg class="t4s-btn-icon" viewBox="0 0 14 10">
                            <use xlink:href="#t4s-icon-btn"></use>
                          </svg>
                        {%- endif -%}
                      </a>
                    </p>
                  {%- endif -%}
                {%- endfor -%}
              {%- endif -%}
            </div>
            <div data-personalization-id="00007" class=" apz-widget-root-container"></div>
          {%- endif -%}
        </div>
      </div>
    </div>
    <div class="t4s-drawer__bottom">
      <div data-cart-discounts>
        {%- if cart.cart_level_discount_applications != blank -%}
          <ul class="t4s-cart_discounts">
            {%- for discount_application in cart.cart_level_discount_applications -%}
              <li class="t4s-order_cart_discounts">
                <svg viewBox="0 0 24 24" width="20">
                  <use href="#icon-cart-tag"/>
                </svg>
                {{- discount_application.title -}}
                (-{{ discount_application.total_allocated_amount | money }})
              </li>
            {%- endfor -%}
          </ul>
        {%- endif -%}
      </div>
      {%- for block in section_blocks -%}
        {%- assign bk_stts = block.settings -%}
        {%- assign button_style = bk_stts.button_style -%}
        {%- case block.type -%}
          {%- when 'price' -%}
            <div
              class="t4s-cart-total t4s-row t4s-gx-5 t4s-gy-0 t4s-align-items-center t4s-justify-content-between"
              {{ block.shopify_attributes }}
            >
              <div class="t4s-col-auto --subtotal-label">
                <strong>{{ 'cart.mini_cart.subtotal' | t }}</strong>
              </div>
              <div data-cart-prices class="t4s-col-auto t4s-text-right">
                <div class="t4s-cart__totalPrice">{{ total_price | money }}</div>
              </div>
            </div>

            {%- comment -%}
              保障服务行（Accident / Shipping Protection）。
              只渲染 type 为 'protection' 的 block，未配置时不输出任何内容，
              因此不会凭空造出价格。图标为 Figma 原始 SVG（14px，圈 #F5F5F5，勾 #FFD000）。
            {%- endcomment -%}
            {%- assign protection_blocks = section_blocks | where: 'type', 'protection' -%}
            {%- if protection_blocks.size > 0 -%}
              <div class="--protection-list">
                {%- for pb in protection_blocks -%}
                  {%- assign pb_stts = pb.settings -%}
                  <div class="--protection-row" {{ pb.shopify_attributes }}>
                    <div class="--protection-head">
                      <div class="--protection-label">
                        {%- if pb_stts.checked -%}
                          <svg viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                            <circle cx="7" cy="7" r="5.6" stroke="#F5F5F5" stroke-width="1.5"/>
                            <path d="M9.29482 5.6249L6.32498 8.59475" stroke="#FFD000" stroke-width="1.5" stroke-linecap="round"/>
                            <path d="M6.32495 8.59478L4.84003 7.10985" stroke="#FFD000" stroke-width="1.5" stroke-linecap="round"/>
                          </svg>
                        {%- else -%}
                          <svg viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                            <circle cx="7" cy="7" r="5.25" stroke="#F5F5F5" stroke-width="1.5"/>
                          </svg>
                        {%- endif -%}
                        <span>{{ pb_stts.label }}</span>
                      </div>
                      <span class="--protection-price">{{ pb_stts.price }}</span>
                    </div>
                    {%- if pb_stts.description != blank -%}
                      <p class="--protection-desc">{{ pb_stts.description }}</p>
                    {%- endif -%}
                  </div>
                {%- endfor -%}
              </div>
            {%- endif -%}
          {%- when 'tax' -%}
            {%- capture taxes_shipping_checkout -%}{%- assign page_url = pages[settings.link_ship].url -%}
                        {%- if se_stts.show_discount -%}
                            {%- if cart.taxes_included and page_url != blank -%}
                              {{ 'cart.general.taxes_included_discounts_and_shipping_policy_html' | t: link: page_url }}
                            {%- elsif cart.taxes_included -%}
                              {{ 'cart.general.taxes_included_discounts_but_shipping_at_checkout' | t }}
                            {%- elsif page_url != blank -%}
                              {{ 'cart.general.taxes_discounts_and_shipping_policy_at_checkout_html' | t: link: page_url }}
                            {%- else -%}
                              {{ 'cart.general.taxes_discounts_and_shipping_at_checkout' | t }}
                            {%- endif -%}
                        {%- else -%}
                            {%- if cart.taxes_included and page_url != blank -%}
                              {{ 'cart.general.taxes_included_and_shipping_policy_html' | t: link: page_url }}
                            {%- elsif cart.taxes_included -%}
                              {{ 'cart.general.taxes_included_but_shipping_at_checkout' | t }}
                            {%- elsif page_url != blank -%}
                              {{ 'cart.general.taxes_and_shipping_policy_at_checkout_html' | t: link: page_url }}
                            {%- else -%}
                              {{ 'cart.general.taxes_and_shipping_at_checkout' | t }}
                            {%- endif -%}
                        {%- endif -%}
                     {%- endcapture -%}
            <p class="t4s-cart__tax" {{ block.shopify_attributes }}>{{ taxes_shipping_checkout }}</p>

          {%- when 'agree' -%}
            {%- capture terms_and_conditions -%}{%- assign page_url = pages[settings.link_conditions].url -%}
                      {%- if page_url != blank -%}
                        {{ 'cart.general.terms_and_conditions_html' | t: link: page_url }}
                      {%- else -%}
                        {{ 'cart.general.terms_and_conditions' | t }}
                      {%- endif -%}
                     {%- endcapture -%}
            <p class="t4s-pr t4s-d-block t4s-cart__agree" {{ block.shopify_attributes }}>
              <input type="checkbox" id="cart_agree" data-agree-checkbox name="{{ck_lumise}}"
              ><label for="cart_agree">{{ terms_and_conditions }}</label>
              <svg class="t4s-dn t4s-icon_checked">
                <use href="#icon-cart-selected"/>
              </svg>
            </p>

          {%- when 'btnc' -%}
            <a
              {{ block.shopify_attributes }}
              href="{{cart_url}}"
              data-loading-bar
              class="t4s-btn__cart t4s-btn t4s-btn-base t4s-btn-style-{{button_style}} t4s-btn-color-{{bk_stts.btn_cl}} {% if button_style == 'default' or button_style == 'outline'%}t4s-btn-effect-{{bk_stts.button_effect}}{% endif %} t4s-w-100 t4s-justify-content-center t4s-truncate"
            >
              {{- 'cart.mini_cart.view' | t }}
              {%- if bk_stts.btn_icon -%}
                <svg class="t4s-btn-icon" viewBox="0 0 14 10">
                  <use xlink:href="#t4s-icon-btn"></use>
                </svg>
              {%- endif -%}
            </a>

          {%- when 'btnck' -%}
            {%- if settings.currency_type == '2' and settings.notify_currency and settings.mess_currency != blank -%}
              {%- assign cart_iso_code = cart.currency.iso_code -%}
              {%- assign text1 = '{{ currency }}' -%}
              <p class="t4s-d-block" data-currency-jsnotify>
                {{
                  settings.mess_currency
                  | replace: text1, cart_iso_code
                  | replace: '[currency]', cart_iso_code
                  | replace: '[original_currency]', cart_iso_code
                  | replace: '[current_currency]', '<span class="selected-currency"></span>'
                }}
              </p>
            {%- endif -%}
            <button
              {{ block.shopify_attributes }}
              type="submit"
              data-loading-bar
              data-confirm="{{ck_lumise}}"
              name="checkout"
              class="t4s-btn__checkout t4s-btn t4s-btn-base"
            >
              {{ 'cart.mini_cart.checkout' | t }}
              {%- if bk_stts.btn_icon -%}
                <svg class="t4s-btn-icon" viewBox="0 0 14 10">
                  <use xlink:href="#t4s-icon-btn"></use>
                </svg>
              {%- endif -%}
            </button>

          {%- when 'btnmr' -%}
            {%- if additional_checkout_buttons and unadmin_sp and settings.enable_sub_reCharge == false -%}
              {{ arr_agree[0] }}
              <div
                {{ block.shopify_attributes }}
                data-add-ckt4
                class="additional_checkout_buttons additional-checkout-buttons--vertical mt__10 mb__10{{cl_agree}}"
              >
                {{ content_for_additional_checkout_buttons }}
              </div>
              {{ arr_agree[1] }}
            {%- endif -%}

          {%- when 'img' -%}
            {% if bk_stts.image == blank %}{% continue %}{% endif -%}
            {%- assign image = bk_stts.image -%}
            <div class="t4s-cat__imgtrust t4s_ratioadapt" {{ block.shopify_attributes }}>
              <div class="t4s_ratio t4s-pr t4s-oh" style="--aspect-ratioapt:{{image.aspect_ratio |default: 1}};">
                <img
                  class="t4s-w-100 lazyloadt4s"
                  data-src="{{ image | image_url: width: 1 }}"
                  data-widths="[100,200,400,600]"
                  data-optimumx="2"
                  data-sizes="auto"
                  src="{% render 'img_svg',w:image.width,h:image.height %}"
                  width="{{ image.width }}"
                  height="{{ image.height }}"
                  alt="{{ image.alt | escape }}"
                >
                <span
                  class="lazyloadt4s-loader is-bg-img"
                  style="background: url({{ image | image_url: width: 1 }});"
                ></span>
              </div>
            </div>

          {%- else -%}
            {% continue -%}
        {%- endcase -%}
      {%- endfor -%}
    </div>
  </form>

  {%- comment -%}
    整车加载态。母主题更新购物车时会给 [data-cart-wrapper]（即本抽屉）
    加 .is--contentUpdate，并用 #t4s-mini_cart:before 把内容压暗，
    但没有加载指示器。这里补一个居中 spinner，同时隐藏单个商品上的那个。
  {%- endcomment -%}
  <div class="cs-cart-loading" aria-hidden="true">
    <svg class="cs-cart-loading__spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
      <circle cx="33" cy="33" r="30" fill="none" stroke-width="6"></circle>
    </svg>
  </div>
</div>

<style>
    /* 隐藏滚动条但保留滚动功能 */
  .hide-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .hide-scrollbar {
    -ms-overflow-style: none;  /* IE 10+ */
    scrollbar-width: none;  /* Firefox */
  }

  @keyframes cs-cart-spin {
    to {
      transform: rotate(360deg);
    }
  }

  /* IMPORTANT: 遮罩的定位与隐藏必须在所有断点生效。
     若只写在移动端媒体查询里，PC 上这个 div 没有任何样式，
     会作为 .t4s-drawer flex 列的普通子项占据空间（实测 463x463）。 */
  #t4s-mini_cart .cs-cart-loading {
    position: absolute;
    inset: 0;
    z-index: 20;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.2s ease-in-out, visibility 0.2s ease-in-out;
  }

  #t4s-mini_cart .cs-cart-loading__spinner {
    width: 40px;
    height: 40px;
    animation: cs-cart-spin 0.8s linear infinite;
  }

  #t4s-mini_cart .cs-cart-loading__spinner circle {
    stroke: #f2cb04;
    stroke-linecap: round;
    stroke-dasharray: 150;
    stroke-dashoffset: 110;
  }

  /* 仅移动端启用整车加载态，PC 保持母主题的单品 spinner */
  @media (max-width: 767.98px) {
    #t4s-mini_cart .t4s-cart-ld__bar {
      display: none !important;
    }

    #t4s-mini_cart.is--contentUpdate .cs-cart-loading {
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
    }
  }
</style>

{%- comment -%}
  Subtotal 显示「商品总价 + 已勾选的保障服务费」。

  为什么必须用 JS：保障服务（Shipping / Accident Protection）由 xcotton App 管理，
  并不是购物车行项目，所以 cart.total_price 永远不含这笔费用（可验算：
  2199 + 1599×4 = 8595，正好等于 Subtotal，不含 $173 保障费）。
  Liquid 拿不到这个金额，只能在前端读取 App 渲染出来的数值再相加。

  内联 script 而非 src/js/sections/：新增 JS 入口需要 npm run build 才会产出
  theme/assets/*.min.js，内联可免构建直接生效。需要时可迁移。

  IMPORTANT: 若下面的启发式没能识别 App 结构，只需把准确选择器填进
  PROTECTION_SELECTORS，其余逻辑无需改动。
{%- endcomment -%}
<script>
  (function () {
    var DRAWER = "#t4s-mini_cart";
    var TOTAL_EL = ".t4s-cart__totalPrice";
    // 已知 App 容器选择器优先；留空则走文案启发式
    var PROTECTION_SELECTORS = [];
    var LABEL_RE = /(shipping|accident|delivery)[\s\-_]*(protection|guarantee|insurance)/i;
    var MONEY_RE = /(?:[$€£¥]|US\$|CA\$)\s*([\d.,]+)/;
    var MONEY_FORMAT = {{ shop.money_format | strip_html | json }};
    var writing = false;

    function formatMoney(cents) {
      var fixed = (cents / 100).toFixed(2).split(".");
      var withSep = fixed[0].replace(/\B(?=(\d{3})+(?!\d))/g, ",");
      return MONEY_FORMAT.replace(/\{\{\s*amount\s*\}\}/g, withSep + "." + fixed[1]).replace(
        /\{\{\s*amount_no_decimals\s*\}\}/g,
        withSep
      );
    }

    // "1,234.56" / "173" -> 分
    function parseMoneyToCents(text) {
      var m = String(text).match(MONEY_RE);
      if (!m) return null;
      var n = parseFloat(m[1].replace(/,/g, ""));
      return isFinite(n) ? Math.round(n * 100) : null;
    }

    function isStruckThrough(el) {
      if (el.closest("del, s, strike")) return true;
      var d = getComputedStyle(el).textDecorationLine || "";
      return d.indexOf("line-through") > -1;
    }

    function isSelected(container) {
      var box = container.querySelector('input[type="checkbox"], input[type="radio"]');
      if (box) return box.checked;
      var sw = container.querySelector('[role="switch"], [aria-checked]');
      if (sw) return sw.getAttribute("aria-checked") === "true";
      return !!container.querySelector(
        '[class*="checked"], [class*="selected"], [class*="is--active"], [class*="active"]'
      );
    }

    function hasToggle(el) {
      return !!el.querySelector(
        'input[type="checkbox"], input[type="radio"], [role="switch"], [aria-checked]'
      );
    }

    function findContainers(root) {
      var found = [];
      PROTECTION_SELECTORS.forEach(function (sel) {
        root.querySelectorAll(sel).forEach(function (el) {
          found.push(el);
        });
      });
      if (found.length) return found;

      // 先定位含保障服务文案的最内层元素
      var labels = [];
      root.querySelectorAll("div, section, li, label, span, p, h1, h2, h3, h4").forEach(function (el) {
        if (!LABEL_RE.test(el.textContent || "")) return;
        if (
          !Array.prototype.some.call(el.children, function (c) {
            return LABEL_RE.test(c.textContent || "");
          })
        ) {
          labels.push(el);
        }
      });

      // 文案元素本身通常只有标题，向上找到同时含金额和开关的区块
      labels.forEach(function (label) {
        var el = label;
        for (var i = 0; i < 8 && el && el !== root; i++) {
          if (hasToggle(el) && readAmountCents(el) !== null) {
            if (found.indexOf(el) === -1) found.push(el);
            return;
          }
          el = el.parentElement;
        }
      });

      // 去重：若某容器包含另一个容器，只保留内层，避免重复计价
      return found.filter(function (a) {
        return !found.some(function (b) {
          return b !== a && a.contains(b);
        });
      });
    }

    // 取容器内「未被划掉」的金额，即实际售价
    function readAmountCents(container) {
      var best = null;
      container.querySelectorAll("*").forEach(function (el) {
        if (el.children.length) return;
        var cents = parseMoneyToCents(el.textContent || "");
        if (cents === null || isStruckThrough(el)) return;
        if (best === null || cents < best) best = cents;
      });
      return best;
    }

    function protectionCents(root) {
      var sum = 0;
      findContainers(root).forEach(function (c) {
        if (!isSelected(c)) return;
        var cents = readAmountCents(c);
        if (cents) sum += cents;
      });
      return sum;
    }

    function apply() {
      var root = document.querySelector(DRAWER);
      if (!root || writing) return;
      var target = root.querySelector(TOTAL_EL);
      if (!target) return;

      fetch("/cart.js", { headers: { Accept: "application/json" } })
        .then(function (r) {
          return r.json();
        })
        .then(function (cart) {
          var next = formatMoney(cart.total_price + protectionCents(root));
          if (target.textContent.trim() === next) return;
          writing = true;
          target.textContent = next;
          setTimeout(function () {
            writing = false;
          }, 0);
        })
        .catch(function () {
          /* 取不到购物车就保留 Liquid 渲染的原值，不显示错误金额 */
        });
    }

    var timer;
    function schedule() {
      clearTimeout(timer);
      timer = setTimeout(apply, 120);
    }

    document.addEventListener("DOMContentLoaded", schedule);
    document.addEventListener("cart:updated", schedule);
    document.addEventListener("change", function (e) {
      if (e.target.closest && e.target.closest(DRAWER)) schedule();
    });
    document.addEventListener("click", function (e) {
      if (e.target.closest && e.target.closest(DRAWER)) schedule();
    });

    var root = document.querySelector(DRAWER);
    if (root && window.MutationObserver) {
      new MutationObserver(function () {
        if (!writing) schedule();
      }).observe(root, { childList: true, subtree: true, characterData: true });
    }
    schedule();
  })();
</script>
{%- schema -%}
{
  "name": "Shopping Cart Widget",
  "max_blocks": 20,
  "settings": [
    {
      "type": "paragraph",
      "content": "Those settings are only applicable when the cart contains at least one product."
    },
    {
      "type": "select",
      "id": "style",
      "options": [
        { "value": "1", "label": "Icon" },
        { "value": "2", "label": "Button text" }
      ],
      "label": "Buttons style"
    },
    /*{
        "type": "select",
        "id": "pos",
        "default": "2",
        "options": [
          {"value": "1","label": "Top"},
          {"value": "2","label": "Bottom"}
        ],
        "info": "Only working with button style icon",
        "label": "Position button icon:"
      },
      {
        "type": "checkbox",
        "id": "enable_pr",
        "label": "Enable product recommendations",
        "default": true
      },*/
    {
      "type": "checkbox",
      "id": "enable_calc_ship",
      "label": "Enable calc shipping",
      "info": "Free shipping minimum amount.",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_note",
      "label": "Enable order notes",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_rates",
      "label": "Enable shipping rates calculator",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_discount",
      "label": "Enable input box discounts codes",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_gift_wrap",
      "label": "Enable gift wrap",
      "default": true
    },
    {
      "type": "header",
      "content": "+ Shipping Rates Calculator"
    },
    {
      "type": "text",
      "id": "ship_df_country",
      "label": "Default country selection",
      "default": "United States"
    },
    {
      "type": "paragraph",
      "content": "If your customer is logged-in, the country in his default shipping address will be selected. If you are not sure about the  spelling to use here, refer to the first checkout page."
    }
  ],
  "blocks": [
    {
      "type": "price",
      "name": "Total price",
      "limit": 1
    },
    {
      "type": "protection",
      "name": "保障服务行",
      "settings": [
        {
          "type": "paragraph",
          "content": "仅移动端购物车抽屉显示，位置固定在小计下方、结算按钮上方。"
        },
        {
          "type": "text",
          "id": "label",
          "label": "名称",
          "default": "Accident Protection"
        },
        {
          "type": "text",
          "id": "price",
          "label": "价格",
          "info": "按展示文本填写，例如 $148.00",
          "default": "$148.00"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "描述",
          "default": "Protect your bike from unexpected accidental damage."
        },
        {
          "type": "checkbox",
          "id": "checked",
          "label": "显示为已勾选",
          "info": "当前仅控制图标样式，不会把服务加入购物车。",
          "default": false
        }
      ]
    },
    {
      "type": "tax",
      "name": "Taxes and shipping info",
      "limit": 1,
      "settings": [
        {
          "type": "richtext",
          "id": "image",
          "label": "Content"
        }
      ]
    },
    {
      "type": "agree",
      "name": "Terms,conditions checkbox",
      "limit": 1
    },
    {
      "type": "btnc",
      "name": "Button Cart",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "btn_icon",
          "label": "Enable button icon",
          "default": false
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button style",
          "options": [
            {
              "label": "Default",
              "value": "default"
            },
            {
              "label": "Outline",
              "value": "outline"
            },
            {
              "label": "Bordered bottom",
              "value": "bordered"
            },
            {
              "label": "Link",
              "value": "link"
            }
          ]
        },
        {
          "type": "select",
          "id": "btn_cl",
          "label": "Button color",
          "default": "dark",
          "options": [
            {
              "value": "light",
              "label": "Light"
            },
            {
              "value": "dark",
              "label": "Dark"
            },
            {
              "value": "primary",
              "label": "Primary"
            },
            {
              "value": "custom1",
              "label": "Custom color 1"
            },
            {
              "value": "custom2",
              "label": "Custom color 2"
            }
          ]
        },
        {
          "type": "select",
          "id": "button_effect",
          "label": "Button hover effect",
          "default": "default",
          "info": "Only working button style default, outline",
          "options": [
            {
              "label": "Default",
              "value": "default"
            },
            {
              "label": "Fade",
              "value": "fade"
            },
            {
              "label": "Rectangle out",
              "value": "rectangle-out"
            },
            {
              "label": "Sweep to right",
              "value": "sweep-to-right"
            },
            {
              "label": "Sweep to left",
              "value": "sweep-to-left"
            },
            {
              "label": "Sweep to bottom",
              "value": "sweep-to-bottom"
            },
            {
              "label": "Sweep to top",
              "value": "sweep-to-top"
            },
            {
              "label": "Shutter out horizontal",
              "value": "shutter-out-horizontal"
            },
            {
              "label": "Outline",
              "value": "outline"
            },
            {
              "label": "Shadow",
              "value": "shadow"
            }
          ]
        }
      ]
    },
    {
      "type": "btnck",
      "name": "Button Checkout",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "btn_icon",
          "label": "Enable button icon",
          "default": false
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button style",
          "options": [
            {
              "label": "Default",
              "value": "default"
            },
            {
              "label": "Outline",
              "value": "outline"
            },
            {
              "label": "Bordered bottom",
              "value": "bordered"
            },
            {
              "label": "Link",
              "value": "link"
            }
          ]
        },
        {
          "type": "select",
          "id": "btn_cl",
          "label": "Button color",
          "default": "dark",
          "options": [
            {
              "value": "light",
              "label": "Light"
            },
            {
              "value": "dark",
              "label": "Dark"
            },
            {
              "value": "primary",
              "label": "Primary"
            },
            {
              "value": "custom1",
              "label": "Custom color 1"
            },
            {
              "value": "custom2",
              "label": "Custom color 2"
            }
          ]
        },
        {
          "type": "select",
          "id": "button_effect",
          "label": "Button hover effect",
          "default": "default",
          "info": "Only working button style default, outline",
          "options": [
            {
              "label": "Default",
              "value": "default"
            },
            {
              "label": "Fade",
              "value": "fade"
            },
            {
              "label": "Rectangle out",
              "value": "rectangle-out"
            },
            {
              "label": "Sweep to right",
              "value": "sweep-to-right"
            },
            {
              "label": "Sweep to left",
              "value": "sweep-to-left"
            },
            {
              "label": "Sweep to bottom",
              "value": "sweep-to-bottom"
            },
            {
              "label": "Sweep to top",
              "value": "sweep-to-top"
            },
            {
              "label": "Shutter out horizontal",
              "value": "shutter-out-horizontal"
            },
            {
              "label": "Outline",
              "value": "outline"
            },
            {
              "label": "Shadow",
              "value": "shadow"
            }
          ]
        }
      ]
    },
    {
      "type": "img",
      "name": "Image trust",
      "limit": 1,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    },
    {
      "type": "btn",
      "name": "Button emty",
      "settings": [
        {
          "type": "paragraph",
          "content": "Note: Only show when cart empty."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Button title",
          "default": "Return To Shop",
          "info": "If set blank will not show"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Button link"
        },
        {
          "type": "checkbox",
          "id": "btn_icon",
          "label": "Enable button icon",
          "default": false
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button style",
          "options": [
            {
              "label": "Default",
              "value": "default"
            },
            {
              "label": "Outline",
              "value": "outline"
            },
            {
              "label": "Bordered bottom",
              "value": "bordered"
            },
            {
              "label": "Link",
              "value": "link"
            }
          ]
        },
        {
          "type": "select",
          "id": "btn_cl",
          "label": "Button color",
          "default": "dark",
          "options": [
            {
              "value": "light",
              "label": "Light"
            },
            {
              "value": "dark",
              "label": "Dark"
            },
            {
              "value": "primary",
              "label": "Primary"
            },
            {
              "value": "custom1",
              "label": "Custom color 1"
            },
            {
              "value": "custom2",
              "label": "Custom color 2"
            }
          ]
        },
        {
          "type": "select",
          "id": "button_effect",
          "label": "Button hover effect",
          "default": "fade",
          "info": "Only working button style default, outline",
          "options": [
            {
              "label": "Default",
              "value": "default"
            },
            {
              "label": "Fade",
              "value": "fade"
            },
            {
              "label": "Rectangle out",
              "value": "rectangle-out"
            },
            {
              "label": "Sweep to right",
              "value": "sweep-to-right"
            },
            {
              "label": "Sweep to left",
              "value": "sweep-to-left"
            },
            {
              "label": "Sweep to bottom",
              "value": "sweep-to-bottom"
            },
            {
              "label": "Sweep to top",
              "value": "sweep-to-top"
            },
            {
              "label": "Shutter out horizontal",
              "value": "shutter-out-horizontal"
            },
            {
              "label": "Outline",
              "value": "outline"
            },
            {
              "label": "Shadow",
              "value": "shadow"
            }
          ]
        }
      ]
    }
  ],
  "default": {
    "blocks": [
      { "type": "price" },
      { "type": "tax" },
      { "type": "agree" },
      { "type": "btnc" },
      { "type": "btnck" },
      { "type": "img" },
      { "type": "btn" }
    ]
  }
}
{%- endschema -%}